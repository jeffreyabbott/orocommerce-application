{% extends 'OroUIBundle:actions:update.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}
{% form_theme form with 'OroFormBundle:Form:fields.html.twig' %}

{% if form.vars.value.id %}
    {% set formAction = path('donation.application_update', { 'id': form.vars.value.id }) %}
{% else %}
    {% set formAction = path('donation.application_create') %}
{% endif %}

{% block navButtons %}
    {% if form.vars.value.id and is_granted('DELETE', form.vars.value) %}
        {{ UI.deleteButton({
            'dataUrl': path('donation.application_delete', {'id': form.vars.value.id}),
            'dataRedirect': path('donation.application_index'),
            'aCss': 'no-hash remove-button',
            'id': 'btn-remove-tag',
            'dataId': form.vars.value.id,
            'entity_label': 'EmbApplication'|trans
        }) }}
        {{ UI.buttonSeparator() }}
    {% endif %}
    {{ UI.cancelButton(path('donation.application_index')) }}
    {% set html = UI.saveAndCloseButton() %}
    {% if is_granted('donation.application_update') %}
        {% set html = html ~ UI.saveAndStayButton() %}
    {% endif %}
    {{ UI.dropdownSaveButton({ 'html': html }) }}
{% endblock navButtons %}

{% block pageHeader %}
    {% if form.vars.value.id %}
        {% set breadcrumbs = {
            'entity':      form.vars.value,
            'indexPath':   path('donation.application_index'),
            'indexLabel': 'EmbApplications'|trans,
            'entityTitle': form.vars.value.embNumber
        } %}
        {{ parent() }}
    {% else %}
        {% set title = 'oro.ui.create_entity'|trans({'%entityName%': 'EmbApplication'|trans}) %}
        {% include 'OroUIBundle::page_title_block.html.twig' with { title: 'test' } %}
    {% endif %}
{% endblock pageHeader %}

{% block content_data %}
    {% set id = 'donationapplication-edit' %}

    {% set dataBlocks = [{
        'title': 'General'|trans,
        'class': 'active',
        'subblocks': [{
            'title': '',
            'data': [
                form_row(form.id),
                form_row(form.applicationId),
                form_row(form.medicalProblems),
                form_row(form.medicalProblemsText),
                form_row(form.surgeries),
                form_row(form.surgeriesText),
                form_row(form.currentMedications),
                form_row(form.currentMedicationsText),
                form_row(form.allergies),
                form_row(form.allergiesText),
                form_row(form.smoker),
                form_row(form.smokerWillingToQuit),
                form_row(form.smokerHowMuch),
                form_row(form.alcoholUse),
                form_row(form.alcoholUseText),
                form_row(form.glassesContacts),
                form_row(form.hearingProblems),
                form_row(form.hearingProblemsSeverity),
                form_row(form.dentalProblems),
                form_row(form.dentalProblemsSeverity),
                form_row(form.dietRestrictions),
                form_row(form.dietRestrictionsText),
                form_row(form.bloodType),
                form_row(form.historyInfertility),
                form_row(form.historyInfertilityText),
            ]
        }]
    }]
    %}
    {% set data = {
        'formErrors': form_errors(form)? form_errors(form) : null,
        'dataBlocks': dataBlocks,
    } %}
    {{ parent() }}
{% endblock content_data %}